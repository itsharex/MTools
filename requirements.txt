# ============================================
# MTools 项目依赖清单
# ============================================
# 
# 推荐使用 uv 来管理依赖（更快更可靠）：
#   uv sync
#
# 如果使用传统 pip：
#   pip install -r requirements.txt
#
# 注意：本文件从 pyproject.toml 同步
# 主要依赖配置请编辑 pyproject.toml
# ============================================

flet==0.28.3
ffmpeg-python==0.2.0
Pillow==12.0.0
chardet==5.2.0
opencv-python-headless==4.11.0.86
numpy==1.26.4

# ONNX Runtime - AI 功能 GPU 加速
# 使用平台标记自动选择合适的版本
onnxruntime-directml==1.22.0 ; sys_platform == 'win32'  # Windows DirectML 加速
onnxruntime==1.22.0 ; sys_platform == 'linux' or sys_platform == 'darwin'  # macOS (内置 CoreML) / Linux (可手动改为 onnxruntime-gpu)
# 完整加速独立环境，无需用户手动配置cuda和cudnn，直接使用onnxruntime-gpu[cuda,cudnn]==1.22.0即可，但是会导致依赖包体积增大，不推荐使用

websockets==12.0  # 避免弃用警告，等待 flet 更新
qrcode[pil]==7.4.2